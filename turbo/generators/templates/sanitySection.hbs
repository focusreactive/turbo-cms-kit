import SectionContainer from '@/components/SectionContainer';
import type { I{{ capitialize sectionName }}SectionProps } from './types';
import { {{ capitialize sectionName }} } from '@shared/ui'

export default function {{ capitialize sectionName }}Section({ data }: I{{ capitialize sectionName }}SectionProps) {
  if (!data) return null;

  const { title } = data;

  return (
    <SectionContainer sectionData={data}>
      <{{ capitialize sectionName }} title={title} />
    </SectionContainer>
  )
}
