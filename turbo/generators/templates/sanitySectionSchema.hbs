import { defineField } from 'sanity'

export default {
  options: {},
  name: 'section.{{ sectionName }}',
  title: '{{ capitialize sectionName }}',
  type: 'object',
  fields: [
    defineField({
        name: 'title',
        type: 'string',
      }),
  ],
}
